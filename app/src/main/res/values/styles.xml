<resources>

    <style name="AppTheme.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenAnimationDuration">3000</item> <!-- only 3 seconds -->
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>

    <style name="Widget.AppTheme.MonthChip" parent="Widget.Material3.Chip.Filter">
        <item name="android:checkable">true</item>
        <item name="checkedIconEnabled">false</item>
        <item name="checkedIconVisible">false</item>
        <item name="chipBackgroundColor">@color/background_color_month</item>
        <item name="android:textAppearance">@style/TextAppearance.Material3.LabelMedium</item>
        <item name="android:textColor">@color/on_background_color_month</item>
    </style>

</resources>
