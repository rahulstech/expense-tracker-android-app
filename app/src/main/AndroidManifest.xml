<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dreammaker.android.expensetracker">

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar">

        <!-- START: activities for backup and restore -->

        <activity android:name="rahulstech.android.expensetracker.backuprestore.ui.BackupRestoreSettingsHomeActivity"
            android:label="@string/title_app_data"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ui.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.main.MainActivity" />
        </activity>

        <activity android:name="rahulstech.android.expensetracker.backuprestore.ui.ActivityPickBackupFile"
            android:label="@string/title_local_restore"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <!-- END: activities for backup and restore -->

        <activity
            android:name=".ui.splash.SplashActivity"
            android:noHistory="true"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- START: app settings -->

        <activity
            android:name="dreammaker.android.expensetracker.settings.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.main.MainActivity" />
        </activity>

        <!-- START: app settings -->

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"/>
    </application>

</manifest>